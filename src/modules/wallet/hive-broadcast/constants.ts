/** Hive Engine sidechain `custom_json.id` for token contract actions. */
export const HIVE_ENGINE_CUSTOM_JSON_ID = 'ssc-mainnet-hive';

export const HIVE_ENGINE_TOKENS_CONTRACT = 'tokens';

/** Max UTF-8 byte length for a single Hive `custom_json.json` payload. */
export const HIVE_CUSTOM_OP_DATA_MAX_LENGTH = 8192;
